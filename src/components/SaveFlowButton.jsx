import React from "react";

const SaveFlowButton = ({ onClick }) => (
  <button onClick={onClick} style={{ marginRight: "10px", padding: "8px 12px" }}>
    Save Flow
  </button>
);

export default SaveFlowButton;
