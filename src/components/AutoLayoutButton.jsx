import React from "react";

const AutoLayoutButton = ({ onClick }) => (
  <button onClick={onClick} style={{ marginRight: "10px", padding: "8px 12px" }}>
    Auto Layout
  </button>
);

export default AutoLayoutButton;
