import React from "react";

const LoadFlowButton = ({ onClick }) => (
  <button onClick={onClick} style={{ marginRight: "10px", padding: "8px 12px" }}>
    Load Flow
  </button>
);

export default LoadFlowButton;
